/*
1) O código abaixo contém a declaração de algumas variáveis e ponteiros. Quando você executa esseCódigo, quais serão os valores de x, y e p ao final do trecho de código abaixo?
*/
void main()
{
    int x, y, *p;
    y = 0;
    x = *p;
    x = 4;
    (*p)++;
    --X;
    (*p) += x;
}
/*
Resp. : x : 3 y : 5 p : Aponta para o endereço de y
  */ 